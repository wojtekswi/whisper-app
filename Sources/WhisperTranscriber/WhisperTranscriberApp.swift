import SwiftUI

@main
struct WhisperTranscriberApp: App {
    var body: some Scene {
        WindowGroup("Whisper Transcriber") {
            ContentView()
        }
        .defaultSize(width: 800, height: 560)
    }
}
